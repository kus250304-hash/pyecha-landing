# CLAUDE.md

이 문서는 전국 3,500개 동(洞) 단위 폐차 비교매입 랜딩페이지를 생성하는 프로그래매틱 SEO 프로젝트의 콘텐츠 원칙을 정의합니다. 페이지를 새로 만들거나 수정할 때 아래 원칙을 반드시 준수합니다.

## 서비스 성격

- 이 서비스는 폐차장을 직접 운영하지 않으며, **전국 폐차 협력업체 네트워크와 연계**하여 상담과 연결을 도와주는 중개형 서비스입니다.
- 핵심 컨셉은 "폐차 대비 수출 비교매입"입니다. 차량 상태와 조건에 따라 국내 폐차 처리와 수출 비교매입 중 더 적합한 방법으로 협력업체와 연결해 준다는 개념을 페이지에 담습니다.
- 특정 업체명·사업자 정보를 페이지 전면에 내세우지 않습니다. 대표번호는 **1600-6011**로 모든 페이지에서 통일합니다.
- 헤더 또는 푸터에 블로그(폐차119, https://blog.naver.com/abh77790)와 유튜브(http://www.youtube.com/@pecha-export) 링크를 아이콘 형태로 포함합니다. 당근마켓 링크는 추후 추가 예정이므로 현재는 넣지 않습니다.

## 콘텐츠 원칙

### 1. 시세·경쟁사 정보 금지
- 폐차 시세, 견적 금액, 매입가 등 구체적인 가격 정보를 페이지에 절대 포함하지 않습니다.
- 특정 경쟁사명, 타사 서비스 비교, 타사 링크를 언급하지 않습니다.
- "폐차 대비 수출 비교매입" 개념은 방식(폐차 처리 vs 수출) 비교로만 서술하고, 구체적인 금액이나 차액을 제시하지 않습니다.

### 2. 중개/연계 서비스임을 정직하게 고지
- 폐차장을 직접 운영하는 것이 아니라 전국 폐차 협력업체 네트워크와 연계된 서비스임을 페이지 안에 고지합니다(예: "전국 폐차 협력업체 네트워크와 함께합니다").
- 이 고지는 완전히 생략하지 않되, 눈에 띄는 강조 박스가 아니라 페이지 하단(FAQ와 푸터 사이)에 작은 회색 글씨로 자연스럽게 배치합니다. 문구도 "직접 운영하지 않으며"처럼 단정적으로 부정하는 표현보다 "협력업체 네트워크와 함께합니다"처럼 부드러운 표현을 사용합니다.

### 3. 실제 서비스 가능 지역 문구 및 출장 방문 서비스 안내 필수
- 해당 동/지역이 실제 서비스 가능 지역임을 명시하는 문구를 반드시 포함합니다.
- 출장 방문 서비스(현장 방문 폐차 처리, 협력업체 방문)에 대한 안내를 반드시 포함합니다.

### 4. 페이지별 최소 분량 및 지역 고유 콘텐츠 필수
- 각 페이지는 최소 분량 기준을 충족해야 합니다(동일 템플릿의 복사·붙여넣기 수준의 얇은 콘텐츠 금지).
- 다음 지역 고유 콘텐츠를 반드시 포함합니다:
  - 해당 지역의 랜드마크/지리적 특징 언급
  - 지역 관련 이용 후기(리뷰)
  - 지역 맞춤 FAQ

### 5. 결과를 약속하는 표현 금지
- "최고가 보장", "무조건 더 드립니다", "100%", "1위", "보장"처럼 결과를 약속하거나 검증할 수 없는 표현은 사용하지 않습니다.
- "최고가 도전", "최고가를 받으실 수 있도록", "수출 시세와 비교", "상담 후 확인"처럼 방향을 말하는 표현은 적극 사용합니다. 목표는 문의를 최대한 많이 끌어내는 것이며, 전화하고 싶어지게 씁니다.
- 비교매입이 항상 더 이득이라는 식의 단정은 하지 않습니다. "폐차보다 수출이 더 받는 차가 있습니다"처럼 조건부로 씁니다.

## 페이지 생성 방법 (v2)

페이지는 `templates/region-landing-v2.html` 하나로만 만듭니다. 지역별 생성 스크립트(generate_batchN_pages.py 류)나 다른 템플릿을 새로 만들지 않습니다.

- 지역 데이터는 `data/regions.json` 한 곳에 있습니다(법정동코드 `code` 포함). 페이지 HTML을 직접 쓰거나 고치지 않고, 데이터를 고친 뒤 `python3 scripts/build_site.py --only <slug,...>` 로 다시 렌더링합니다.
- 새 지역 추가 순서:
  1. `python3 scripts/pick_next_regions.py` → `data/batches/YYYY-MM-DD.json` 뼈대 생성 (개수는 `data/generation_config.json`의 `daily_count`)
  2. 뼈대의 `landmark_name`, `landmark_desc`, `service_intro`, `faqs`(질문·답 쌍 4개 이상), `meta` 를 채움
  3. `python3 scripts/import_batch.py data/batches/YYYY-MM-DD.json` → regions.json 반영, 렌더링, `index.html`·`sitemap.xml` 갱신, `check_pages.py` 검사까지 한 번에 실행. 검사가 실패하면 배치 파일을 고치고 다시 실행
- `python3 scripts/check_pages.py` 는 언제든 전체 페이지를 검사합니다. 오류가 있는 상태로 PR을 올리지 않습니다.
- 생성 결과는 항상 브랜치에 커밋하고 PR로 올립니다. main에 직접 push하지 않습니다.

## 사례(후기) 운영 방법

- 사례 원본(사진·메모)은 운영자가 PC에서 정리해 `cases/input/<사례 폴더>/` 에 올립니다. 형식은 `cases/input/README.md` 에 있습니다(메모.txt 의 지역·차종·상황·처리 + 번호판을 가린 사진).
- `python3 scripts/build_cases.py` 가 그 폴더를 읽어 `cases/<slug>.html` 사례 페이지와 `cases/index.json` 목록을 만들고, 사진을 `cases/images/` 에 줄여 넣고, 처리한 폴더를 `cases/done/` 으로 옮긴 뒤 지역 페이지 전체를 다시 렌더링합니다.
- 사례 글은 메모에 적힌 사실만 씁니다. 스크립트는 메모에 금액·시세·결과 약속 표현이 있으면 그 사례를 처리하지 않습니다. 사진은 운영자가 번호판·얼굴·서류를 가린 것만 올린다고 전제하며, 스크립트가 EXIF·GPS·XMP 같은 숨은 정보를 전부 지운 새 파일로 저장하고 `check_pages.py` 가 남은 정보가 있으면 오류로 막습니다.
- 지역 페이지의 사례 칸: 카드마다 "○○구 ○○동 작업 사례"처럼 실제 지역을 표시합니다. 사례는 그 동 페이지와 같은 시군구(세종은 시 전체)의 다른 동 페이지 최대 `cases_extra_pages_per_case`(site_config.json, 기본 5)곳에만 보이고, 다른 시군구·시도에는 보이지 않습니다(`build_site.py` 의 `case_page_map`·`cases_for`).
- 사례 페이지(`templates/case.html`)에도 지역 페이지와 같은 전화·문자 버튼, 하단 고정 바, 협력업체 고지가 들어갑니다.

### 지역 콘텐츠 작성 규칙 (뼈대를 채울 때)
- `landmark_name`: 그 동에 실제로 있는 잘 알려진 장소 하나(역, 시장, 대학, 공원, 관공서, 큰길 등). 확실하지 않으면 "○○동 주민센터"나 동을 지나는 큰 도로처럼 틀릴 수 없는 것을 씁니다. 지어내지 않습니다. 장소 이름만 쓰고 "인근", "주변", "일대", "근처" 같은 위치 표현이나 "원도심 주택가" 같은 설명은 붙이지 않습니다(템플릿이 "○○ 인근 출장 방문"처럼 뒤에 말을 붙이므로 겹칩니다). 설명은 `landmark_desc`에 씁니다.
- `landmark_desc`: 랜드마크와 동네 특징(주거·상업·공업, 지형, 주차 환경 등) 1~2문장.
- `service_intro`: "{시도} {시군구} {동} 일대는 전국 폐차 협력업체 네트워크와 연계되어 상담과 방문 처리가 가능한 지역입니다."로 시작하고, 그 동의 지형·주거 형태에 맞춘 방문 안내 1~2문장을 이어 씁니다.
- `faqs`: 4~5개. 그중 3개 이상은 질문이나 답에 그 동 이름 또는 랜드마크 이름이 들어가야 합니다(주차 환경, 골목 진입, 방문 시간, 인근 동 방문 가능 여부 등).
- `meta`: "{시도} {시군구} {동} 폐차 비교매입 상담 및 출장 방문 서비스 안내 페이지입니다." 형태 1문장.
- 다른 시도의 지역 이름을 쓰지 않습니다. 인근 동을 언급할 때는 같은 시군구 안의 실제 법정동만 씁니다.
- 시세·금액·경쟁사 언급 금지, 결과를 약속하는 표현 금지(위 콘텐츠 원칙과 동일).
